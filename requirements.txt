# ============================================================
#  GI Agent dependencies
#
#  Install:  pip install -r requirements.txt
#  (Python 3.10+; on Windows you also need BetterGI itself,
#   see README "Quick start")
#
#  NOTE: keep this file ASCII-only. pip reads requirements files
#  with the system locale encoding, and on a Chinese Windows that
#  is GBK -- UTF-8 Chinese comments here make pip die with
#  "UnicodeDecodeError: 'gbk' codec can't decode byte ...".
#  The Chinese explanations live in README.md / docs/ instead.
# ============================================================

# ---------- required: the agent itself (QQ / Feishu server, Studio backend, BetterGI automation) ----------
Flask==3.1.3                # Studio backend + Feishu webhook server
Werkzeug==3.1.8             # used directly by app_web.py (WSGI helpers)
openai==2.32.0              # LLM client (any OpenAI-compatible endpoint: DeepSeek / GitHub Models / NVIDIA / local)
httpx==0.28.1               # QQ open platform, MiYouShe API, LLM client
requests==2.33.1            # showcase / dictionary fetching
urllib3==2.6.3              # retry adapter (imported directly by env_reader / dict_updater)
python-dotenv==1.2.2        # reads .env
websockets==16.0            # QQ bot websocket connection

# ---------- optional: skip them if you do not need the feature ----------
# Feishu channel. api/feishu_api.py imports it lazily, so the agent still runs without it
# (only Feishu messages will fail, with a clear message).
lark-oapi==1.5.5
# Offline dictionary scrapers (skills/*_scraper.py). Not needed to run the agent.
# After installing, download the browser: playwright install chromium
playwright==1.58.0
